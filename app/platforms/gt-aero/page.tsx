// PLACEHOLDER — GT-AERO platform page. Replace with full content when ready.
export default function GtAeroPage() {
  return (
    <main
      style={{
        minHeight: "100dvh",
        backgroundColor: "var(--color-void)",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1
        style={{
          fontFamily: "var(--font-display)",
          fontSize: "clamp(48px, 8vw, 120px)",
          fontWeight: 400,
          letterSpacing: "-0.02em",
          color: "var(--color-text)",
          margin: 0,
        }}
      >
        GT-AERO
      </h1>
    </main>
  );
}
